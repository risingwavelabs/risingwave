# Customer Notes — Avro Enterprise Demo

## Target date posture
Current target date for the agreed demo scope: **Friday, April 24, 2026**, pending final rehearsal sign-off.

## Included scope reminder
The agreed demo scope includes Kafka Avro ingestion, latest-state materialization, tombstone handling, complex Avro, streaming joins/UDFs, native `SUBSCRIBE`, JSON-format downstream output, Postgres CDC, team-based access restrictions, observability, and the agreed failure-handling scenarios.

## Excluded or deferred items
- remove optional field
- AD group to RBAC mapping integration
- real higher-environment promotion during the session
- CSV output
- XML output
- fixed-width output for now

## Fixed-width remark
Fixed-width output was requested originally, but it is intentionally deferred from the current demo scope.

## Narrated-only boundaries
- CI/CD promotion is presented only as a definition-as-code / dbt-compatible automation path in this demo.
- AD group to RBAC mapping is a workaround / future path topic only, not a live integration claim in this session.
