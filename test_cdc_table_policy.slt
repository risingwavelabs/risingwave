statement ok
CREATE SOURCE mysql_source (
    id bigint,
    name varchar
) WITH (
    connector = 'mysql-cdc',
    hostname = 'localhost',
    port = '3306',
    username = 'root',
    password = '123456',
    database.name = 'testdb',
    table.name = 'test_table',
    schema.change.failure.policy = 'Block'
);

statement ok
CREATE TABLE test_table (
    id bigint,
    name varchar,
    PRIMARY KEY (id)
) FROM mysql_source TABLE 'testdb.test_table';

query T
SELECT * FROM test_table;
----
