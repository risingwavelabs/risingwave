# This file is automatically generated by `src/sqlparser/tests/parser_test.rs`.
- input: select * from t match_recognize (pattern (a) define a as a.x > 0)
  formatted_sql: SELECT * FROM t MATCH_RECOGNIZE (PATTERN (a) DEFINE a AS a.x > 0)
- input: select * from t match_recognize (partition by p order by ts measures first(a.x) as fx, last(b.y) as ly one row per match after match skip past last row pattern (a b+ c?) define a as a.x > 0, b as b.y < 1, c as c.z = 2) as mr
  formatted_sql: SELECT * FROM t MATCH_RECOGNIZE (PARTITION BY p ORDER BY ts MEASURES first(a.x) AS fx, last(b.y) AS ly ONE ROW PER MATCH AFTER MATCH SKIP PAST LAST ROW PATTERN (a b+ c?) DEFINE a AS a.x > 0, b AS b.y < 1, c AS c.z = 2) AS mr
- input: select * from t match_recognize (partition by p1, p2 order by ts all rows per match after match skip to next row pattern (a{2} b{1,3} c{2,} d{,4}) define a as a.x > 0)
  formatted_sql: SELECT * FROM t MATCH_RECOGNIZE (PARTITION BY p1, p2 ORDER BY ts ALL ROWS PER MATCH AFTER MATCH SKIP TO NEXT ROW PATTERN (a{2} b{1,3} c{2,} d{,4}) DEFINE a AS a.x > 0)
- input: select * from t match_recognize (pattern ((a | b) c*) define a as a.x > 0, b as b.x < 0, c as c.x = 0)
  formatted_sql: SELECT * FROM t MATCH_RECOGNIZE (PATTERN ((a | b) c*) DEFINE a AS a.x > 0, b AS b.x < 0, c AS c.x = 0)
- input: select * from t match_recognize (pattern (permute(a, b, c)) define a as a.x > 0, b as b.x > 0, c as c.x > 0)
  formatted_sql: SELECT * FROM t MATCH_RECOGNIZE (PATTERN (PERMUTE(a, b, c)) DEFINE a AS a.x > 0, b AS b.x > 0, c AS c.x > 0)
- input: select * from t match_recognize (after match skip to first a pattern (a b) define a as a.x > 0, b as b.x > 0)
  formatted_sql: SELECT * FROM t MATCH_RECOGNIZE (AFTER MATCH SKIP TO FIRST a PATTERN (a b) DEFINE a AS a.x > 0, b AS b.x > 0)
- input: select * from t match_recognize (after match skip to last b pattern (a b) define a as a.x > 0, b as b.x > 0)
  formatted_sql: SELECT * FROM t MATCH_RECOGNIZE (AFTER MATCH SKIP TO LAST b PATTERN (a b) DEFINE a AS a.x > 0, b AS b.x > 0)
- input: select * from t match_recognize (define a as a.x > 0)
  error_msg: |-
    sql parser error: expected PATTERN, found: define
    LINE 1: select * from t match_recognize (define a as a.x > 0)
                                             ^
- input: select * from t match_recognize (pattern (a))
  error_msg: |-
    sql parser error: expected DEFINE, found: )
    LINE 1: select * from t match_recognize (pattern (a))
                                                        ^
