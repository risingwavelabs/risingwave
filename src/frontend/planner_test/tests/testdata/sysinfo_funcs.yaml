# This file is automatically generated. See `src/frontend/planner_test/README.md` for more information.
- sql: |
    select current_schema();
  batch_plan: |
    BatchValues { rows: [['public':Varchar]] }
- sql: |
    select current_schema;
  batch_plan: |
    BatchValues { rows: [['public':Varchar]] }
- sql: |
    select session_user;
  batch_plan: |
    BatchValues { rows: [['root':Varchar]] }
- sql: |
    select current_schemas(true);
  batch_plan: |
    BatchValues { rows: [[ARRAY[pg_catalog, public]:List(Varchar)]] }
- sql: |
    select current_schemas(false);
  batch_plan: |
    BatchValues { rows: [[ARRAY[public]:List(Varchar)]] }
- sql: |
    select current_schemas(null);
  batch_plan: |
    BatchValues { rows: [[null:List(Varchar)]] }
- sql: |
    select current_schemas(true and false);
  binder_error: |-
    Bind error: failed to bind expression: current_schemas(true AND false)

    Caused by:
      Feature is not yet implemented: Only boolean literals are supported in `current_schemas`.
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- sql: |
    select current_timestamp > now() - interval '10 minutes';
  batch_plan: |
    BatchProject { exprs: [(Now > (AtTimeZone((AtTimeZone(Now, 'UTC':Varchar) - '00:00:00':Interval), 'UTC':Varchar) - '00:10:00':Interval)) as $expr1] }
    └─BatchValues { rows: [[]] }
