# This file is automatically generated. See `src/frontend/planner_test/README.md` for more information.
- sql: |
    set enable_locality_backfill = true;
    create table t (a int, b int, c int);
    select count(*) from t group by b;
  stream_plan: |-
    StreamMaterialize { columns: [count, t.b(hidden)], stream_key: [t.b], pk_columns: [t.b], pk_conflict: NoCheck }
    └─StreamProject { exprs: [count, t.b] }
      └─StreamHashAgg { group_key: [t.b], aggs: [count] }
        └─StreamLocalityProvider { locality_columns: [t.b] }
          └─StreamExchange { dist: HashShard(t.b) }
            └─StreamTableScan { table: t, columns: [t.b, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- sql: |
    set enable_locality_backfill = true;
    create table t1 (a int, b int, c int);
    create table t2 (a int, b int, c int);
    select count(*) from t1 join t2 on t1.a = t2.a group by t1.b;
  stream_plan: |-
    StreamMaterialize { columns: [count, t1.b(hidden)], stream_key: [t1.b], pk_columns: [t1.b], pk_conflict: NoCheck }
    └─StreamProject { exprs: [count, t1.b] }
      └─StreamHashAgg { group_key: [t1.b], aggs: [count] }
        └─StreamLocalityProvider { locality_columns: [t1.b] }
          └─StreamExchange { dist: HashShard(t1.b) }
            └─StreamHashJoin { type: Inner, predicate: t1.a = t2.a, output: [t1.b, t1.a, t1._row_id, t2.a, t2._row_id] }
              ├─StreamExchange { dist: HashShard(t1.a) }
              │ └─StreamLocalityProvider { locality_columns: [t1.a] }
              │   └─StreamExchange { dist: HashShard(t1.a) }
              │     └─StreamTableScan { table: t1, columns: [t1.a, t1.b, t1._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t1._row_id], pk: [_row_id], dist: UpstreamHashShard(t1._row_id) }
              └─StreamExchange { dist: HashShard(t2.a) }
                └─StreamLocalityProvider { locality_columns: [t2.a] }
                  └─StreamExchange { dist: HashShard(t2.a) }
                    └─StreamTableScan { table: t2, columns: [t2.a, t2._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t2._row_id], pk: [_row_id], dist: UpstreamHashShard(t2._row_id) }
- sql: |
    set enable_locality_backfill = true;
    create table t (a int, b int, c int, primary key (b, a));
    select count(*) from t group by a, b;
  stream_plan: |-
    StreamMaterialize { columns: [count, t.a(hidden), t.b(hidden)], stream_key: [t.a, t.b], pk_columns: [t.a, t.b], pk_conflict: NoCheck }
    └─StreamProject { exprs: [count, t.a, t.b] }
      └─StreamHashAgg { group_key: [t.a, t.b], aggs: [count] }
        └─StreamLocalityProvider { locality_columns: [t.a, t.b] }
          └─StreamExchange [no_shuffle] { dist: UpstreamHashShard(t.b, t.a) }
            └─StreamTableScan { table: t, columns: [t.a, t.b], stream_scan_type: SnapshotBackfill, stream_key: [t.b, t.a], pk: [b, a], dist: UpstreamHashShard(t.b, t.a) }
- sql: |
    set enable_locality_backfill = true;
    create table t (a int, b int, c int);
    select count(*) from t where c > 1 group by a, b;
  stream_plan: |-
    StreamMaterialize { columns: [count, t.a(hidden), t.b(hidden)], stream_key: [t.a, t.b], pk_columns: [t.a, t.b], pk_conflict: NoCheck }
    └─StreamProject { exprs: [count, t.a, t.b] }
      └─StreamHashAgg { group_key: [t.a, t.b], aggs: [count] }
        └─StreamLocalityProvider { locality_columns: [t.a, t.b] }
          └─StreamExchange { dist: HashShard(t.a, t.b) }
            └─StreamProject { exprs: [t.a, t.b, t._row_id] }
              └─StreamFilter { predicate: (t.c > 1:Int32) }
                └─StreamTableScan { table: t, columns: [t.a, t.b, t._row_id, t.c], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- sql: |
    set enable_locality_backfill = true;
    create table t1 (a int, b int, c int);
    create table t2 (a int, b int, c int);
    select count(*) from t1 join t2 on t1.a = t2.a where t1.c > t2.c group by t1.b;
  stream_plan: |-
    StreamMaterialize { columns: [count, t1.b(hidden)], stream_key: [t1.b], pk_columns: [t1.b], pk_conflict: NoCheck }
    └─StreamProject { exprs: [count, t1.b] }
      └─StreamHashAgg { group_key: [t1.b], aggs: [count] }
        └─StreamLocalityProvider { locality_columns: [t1.b] }
          └─StreamExchange { dist: HashShard(t1.b) }
            └─StreamProject { exprs: [t1.b, t1.a, t1._row_id, t2.a, t2._row_id] }
              └─StreamFilter { predicate: (t1.c > t2.c) }
                └─StreamHashJoin { type: Inner, predicate: t1.a = t2.a, output: all }
                  ├─StreamExchange { dist: HashShard(t1.a) }
                  │ └─StreamLocalityProvider { locality_columns: [t1.a] }
                  │   └─StreamExchange { dist: HashShard(t1.a) }
                  │     └─StreamTableScan { table: t1, columns: [t1.a, t1.b, t1.c, t1._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t1._row_id], pk: [_row_id], dist: UpstreamHashShard(t1._row_id) }
                  └─StreamExchange { dist: HashShard(t2.a) }
                    └─StreamLocalityProvider { locality_columns: [t2.a] }
                      └─StreamExchange { dist: HashShard(t2.a) }
                        └─StreamTableScan { table: t2, columns: [t2.a, t2.c, t2._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t2._row_id], pk: [_row_id], dist: UpstreamHashShard(t2._row_id) }
- sql: |
    set enable_locality_backfill = true;
    create table t (a int, b int, c int);
    select RANK() OVER (PARTITION BY a ORDER BY b) as rank from t;
  stream_plan: |-
    StreamMaterialize { columns: [rank, t.a(hidden), t._row_id(hidden)], stream_key: [t.a, t._row_id], pk_columns: [t.a, t._row_id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [rank, t.a, t._row_id] }
      └─StreamOverWindow { window_functions: [rank() OVER(PARTITION BY t.a ORDER BY t.b ASC ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)] }
        └─StreamLocalityProvider { locality_columns: [t.a] }
          └─StreamExchange { dist: HashShard(t.a) }
            └─StreamTableScan { table: t, columns: [t.a, t.b, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- name: enforce locality for temporal join for both sides.
  sql: |
    set enable_locality_backfill = true;
    create table stream(id1 int, a1 int, b1 int);
    create table version(id2 int, a2 int, b2 int, primary key (id2));
    create index idx2 on version (a2, b2);
    select id1, a1, id2, a2 from stream left join version FOR SYSTEM_TIME AS OF PROCTIME() on a1 = a2 and b1 = b2;
  stream_plan: |-
    StreamMaterialize { columns: [id1, a1, id2, a2, stream.b1(hidden), stream._row_id(hidden)], stream_key: [a1, stream.b1, stream._row_id, id2], pk_columns: [a1, stream.b1, stream._row_id, id2], pk_conflict: NoCheck }
    └─StreamExchange { dist: HashShard(stream.a1, idx2.id2, stream.b1, stream._row_id) }
      └─StreamTemporalJoin { type: LeftOuter, append_only: false, predicate: stream.a1 = idx2.a2 AND stream.b1 = idx2.b2, nested_loop: false, output: [stream.id1, stream.a1, idx2.id2, idx2.a2, stream.b1, stream._row_id] }
        ├─StreamExchange { dist: HashShard(stream.a1) }
        │ └─StreamLocalityProvider { locality_columns: [stream.a1, stream.b1] }
        │   └─StreamExchange { dist: HashShard(stream.a1, stream.b1) }
        │     └─StreamTableScan { table: stream, columns: [stream.id1, stream.a1, stream.b1, stream._row_id], stream_scan_type: ArrangementBackfill, stream_key: [stream._row_id], pk: [_row_id], dist: UpstreamHashShard(stream._row_id) }
        └─StreamExchange [no_shuffle] { dist: UpstreamHashShard(idx2.a2) }
          └─StreamTableScan { table: idx2, columns: [idx2.id2, idx2.a2, idx2.b2], stream_scan_type: UpstreamOnly, stream_key: [idx2.a2, idx2.id2], pk: [a2, b2, id2], dist: UpstreamHashShard(idx2.a2) }
- sql: |
    set enable_locality_backfill = true;
    create table t(a int, b int, c int) append only;
    select distinct on(a) * from t ;
  stream_plan: |-
    StreamMaterialize { columns: [a, b, c, t._row_id(hidden)], stream_key: [a], pk_columns: [a], pk_conflict: NoCheck }
    └─StreamAppendOnlyDedup { dedup_cols: [t.a] }
      └─StreamExchange { dist: HashShard(t.a) }
        └─StreamLocalityProvider { locality_columns: [t.a] }
          └─StreamExchange { dist: HashShard(t.a) }
            └─StreamTableScan { table: t, columns: [t.a, t.b, t.c, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- sql: |
    set enable_locality_backfill = true;
    create table t(a int, b int, c int);
    SELECT * FROM (
    SELECT
        *,
        row_number() OVER (PARTITION BY a ORDER BY b) AS rank
        FROM t
    ) WHERE rank <= 1;
  stream_plan: |-
    StreamMaterialize { columns: [a, b, c, rank], stream_key: [a], pk_columns: [a], pk_conflict: NoCheck }
    └─StreamOverWindow { window_functions: [row_number() OVER(PARTITION BY t.a ORDER BY t.b ASC ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)] }
      └─StreamProject { exprs: [t.a, t.b, t.c] }
        └─StreamGroupTopN { order: [t.b ASC], limit: 1, offset: 0, group_key: [t.a] }
          └─StreamLocalityProvider { locality_columns: [t.a] }
            └─StreamExchange { dist: HashShard(t.a) }
              └─StreamTableScan { table: t, columns: [t.a, t.b, t.c, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- sql: |
    set enable_locality_backfill = true;
    create table t(a int, b int, c int);
    SELECT
      row_number() OVER (PARTITION BY b ORDER BY a) AS rank2
    FROM (
      SELECT * FROM (
        SELECT
            *,
            row_number() OVER (PARTITION BY a, b ORDER BY c) AS rank1
        FROM t
      ) WHERE rank1 <= 1
    );
  stream_plan: |-
    StreamMaterialize { columns: [rank2, t.b(hidden), t.a(hidden)], stream_key: [t.b, t.a], pk_columns: [t.b, t.a], pk_conflict: NoCheck }
    └─StreamProject { exprs: [row_number, t.b, t.a] }
      └─StreamOverWindow { window_functions: [row_number() OVER(PARTITION BY t.b ORDER BY t.a ASC ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)] }
        └─StreamLocalityProvider { locality_columns: [t.b] }
          └─StreamExchange { dist: HashShard(t.b) }
            └─StreamProject { exprs: [t.a, t.b] }
              └─StreamGroupTopN { order: [t.c ASC], limit: 1, offset: 0, group_key: [t.a, t.b] }
                └─StreamLocalityProvider { locality_columns: [t.a, t.b] }
                  └─StreamExchange { dist: HashShard(t.a, t.b) }
                    └─StreamTableScan { table: t, columns: [t.a, t.b, t.c, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- sql: |
    set enable_locality_backfill = true;
    create table t(a int, b int, c int);
    SELECT
      rank1,
      row_number() OVER (PARTITION BY a ORDER BY b) AS rank2
    FROM (
      SELECT
        *,
        row_number() OVER (PARTITION BY a, b ORDER BY c) AS rank1
      FROM t
    );
  stream_plan: |-
    StreamMaterialize { columns: [rank1, rank2, t.a(hidden), t.b(hidden), t._row_id(hidden)], stream_key: [t.a, t.b, t._row_id], pk_columns: [t.a, t.b, t._row_id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [row_number, row_number, t.a, t.b, t._row_id] }
      └─StreamOverWindow { window_functions: [row_number() OVER(PARTITION BY t.a ORDER BY t.b ASC ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)] }
        └─StreamExchange { dist: HashShard(t.a) }
          └─StreamProject { exprs: [t.a, t.b, row_number, t._row_id] }
            └─StreamOverWindow { window_functions: [row_number() OVER(PARTITION BY t.a, t.b ORDER BY t.c ASC ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)] }
              └─StreamLocalityProvider { locality_columns: [t.a, t.b] }
                └─StreamExchange { dist: HashShard(t.a, t.b) }
                  └─StreamTableScan { table: t, columns: [t.a, t.b, t.c, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- sql: |
    set enable_locality_backfill = true;
    create table t(a int, b int, c int);
    SELECT
      rank1,
      row_number() OVER (PARTITION BY b ORDER BY a) AS rank2
    FROM (
      SELECT
        *,
        row_number() OVER (PARTITION BY a, b ORDER BY c) AS rank1
      FROM t
    );
  stream_plan: |-
    StreamMaterialize { columns: [rank1, rank2, t.b(hidden), t.a(hidden), t._row_id(hidden)], stream_key: [t.b, t.a, t._row_id], pk_columns: [t.b, t.a, t._row_id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [row_number, row_number, t.b, t.a, t._row_id] }
      └─StreamOverWindow { window_functions: [row_number() OVER(PARTITION BY t.b ORDER BY t.a ASC ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)] }
        └─StreamLocalityProvider { locality_columns: [t.b] }
          └─StreamExchange { dist: HashShard(t.b) }
            └─StreamProject { exprs: [t.a, t.b, row_number, t._row_id] }
              └─StreamOverWindow { window_functions: [row_number() OVER(PARTITION BY t.a, t.b ORDER BY t.c ASC ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)] }
                └─StreamLocalityProvider { locality_columns: [t.a, t.b] }
                  └─StreamExchange { dist: HashShard(t.a, t.b) }
                    └─StreamTableScan { table: t, columns: [t.a, t.b, t.c, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- sql: |
    set enable_locality_backfill = true;
    create table t (
      id int
    , p1 int
    , p2 int
    , time int
    , v1 int
    , v2 int
    );
    select
        *
        , lag(v1, 0) over (partition by p1 order by id) as out5
        , lag(v1, 1) over (partition by p1, p2 order by id) as out6
        , lag(v1) over (partition by p1, p2 order by time, id) as out7
        , lead(v2, 1) over (partition by p1, p2 order by time, id) as out8
        , lead(v2, 2) over (partition by p1, p2 order by v1, v2) as out9
    from t;
  stream_plan: |-
    StreamMaterialize { columns: [id, p1, p2, time, v1, v2, t._row_id(hidden), out5, out6, out7, out8, out9], stream_key: [p1, p2, t._row_id], pk_columns: [p1, p2, t._row_id], pk_conflict: NoCheck }
    └─StreamOverWindow { window_functions: [first_value(t.v2) OVER(PARTITION BY t.p1, t.p2 ORDER BY t.v1 ASC, t.v2 ASC ROWS BETWEEN 2 FOLLOWING AND 2 FOLLOWING)] }
      └─StreamOverWindow { window_functions: [first_value(t.v1) OVER(PARTITION BY t.p1, t.p2 ORDER BY t.time ASC, t.id ASC ROWS BETWEEN 1 PRECEDING AND 1 PRECEDING), first_value(t.v2) OVER(PARTITION BY t.p1, t.p2 ORDER BY t.time ASC, t.id ASC ROWS BETWEEN 1 FOLLOWING AND 1 FOLLOWING)] }
        └─StreamOverWindow { window_functions: [first_value(t.v1) OVER(PARTITION BY t.p1, t.p2 ORDER BY t.id ASC ROWS BETWEEN 1 PRECEDING AND 1 PRECEDING)] }
          └─StreamLocalityProvider { locality_columns: [t.p1, t.p2] }
            └─StreamExchange { dist: HashShard(t.p1, t.p2) }
              └─StreamOverWindow { window_functions: [first_value(t.v1) OVER(PARTITION BY t.p1 ORDER BY t.id ASC ROWS BETWEEN 0 PRECEDING AND 0 PRECEDING)] }
                └─StreamLocalityProvider { locality_columns: [t.p1] }
                  └─StreamExchange { dist: HashShard(t.p1) }
                    └─StreamTableScan { table: t, columns: [t.id, t.p1, t.p2, t.time, t.v1, t.v2, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- sql: |
    set enable_locality_backfill = true;
    create table t (a int, b int, c int);
    select
      max(num) as max_num
    from (
      select
        count(*) as num, a, b
      from t
      group by a, b
    )
    group by a;
  stream_plan: |-
    StreamMaterialize { columns: [max_num, t.a(hidden)], stream_key: [t.a], pk_columns: [t.a], pk_conflict: NoCheck }
    └─StreamProject { exprs: [max(count), t.a] }
      └─StreamHashAgg { group_key: [t.a], aggs: [max(count), count] }
        └─StreamExchange { dist: HashShard(t.a) }
          └─StreamHashAgg { group_key: [t.a, t.b], aggs: [count] }
            └─StreamLocalityProvider { locality_columns: [t.a, t.b] }
              └─StreamExchange { dist: HashShard(t.a, t.b) }
                └─StreamTableScan { table: t, columns: [t.a, t.b, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- sql: |
    set enable_locality_backfill = true;
    create table t (a int, b int, c int);
    select
      max(num) as max_num
    from (
      select
        count(*) as num, a, b
      from t
      group by a, b
    )
    group by b;
  stream_plan: |-
    StreamMaterialize { columns: [max_num, t.b(hidden)], stream_key: [t.b], pk_columns: [t.b], pk_conflict: NoCheck }
    └─StreamProject { exprs: [max(count), t.b] }
      └─StreamHashAgg { group_key: [t.b], aggs: [max(count), count] }
        └─StreamLocalityProvider { locality_columns: [t.b] }
          └─StreamExchange { dist: HashShard(t.b) }
            └─StreamHashAgg { group_key: [t.a, t.b], aggs: [count] }
              └─StreamLocalityProvider { locality_columns: [t.a, t.b] }
                └─StreamExchange { dist: HashShard(t.a, t.b) }
                  └─StreamTableScan { table: t, columns: [t.a, t.b, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- name: locality provider with sort buffer table attached
  sql: |
    set enable_locality_backfill = true;
    set enable_locality_sort_buffer = true;
    create table t1 (a int, b int, c int);
    create table t2 (a int, b int, c int);
    select count(*) from t1 join t2 on t1.a = t2.a group by t1.b;
  stream_dist_plan: |+
    Fragment 0
    StreamMaterialize { columns: [count, t1.b(hidden)], stream_key: [t1.b], pk_columns: [t1.b], pk_conflict: NoCheck }
    └── StreamProject { exprs: [count, t1.b] }
        └── StreamHashAgg { group_key: [t1.b], aggs: [count] } { tables: [ HashAggState: 0 ] }
            └── StreamLocalityProvider { locality_columns: [t1.b] }
                ├── tables: [ LocalityProviderState: 1, LocalityProviderProgress: 2, LocalityProviderSortBuffer: 3 ]
                └── StreamExchange Hash([0]) from 1

    Fragment 1
    StreamHashJoin { type: Inner, predicate: t1.a = t2.a, output: [t1.b, t1.a, t1._row_id, t2.a, t2._row_id] }
    ├── tables: [ HashJoinLeft: 4, HashJoinDegreeLeft: 5, HashJoinRight: 6, HashJoinDegreeRight: 7 ]
    ├── StreamExchange Hash([0]) from 2
    └── StreamExchange Hash([0]) from 4

    Fragment 2
    StreamLocalityProvider { locality_columns: [t1.a] }
    ├── tables: [ LocalityProviderState: 8, LocalityProviderProgress: 9, LocalityProviderSortBuffer: 10 ]
    └── StreamExchange Hash([0]) from 3

    Fragment 3
    StreamTableScan { table: t1, columns: [t1.a, t1.b, t1._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t1._row_id], pk: [_row_id], dist: UpstreamHashShard(t1._row_id) }
    ├── tables: [ StreamScan: 11 ]
    ├── Upstream
    └── BatchPlanNode

    Fragment 4
    StreamLocalityProvider { locality_columns: [t2.a] } { tables: [ LocalityProviderState: 12, LocalityProviderProgress: 13, LocalityProviderSortBuffer: 14 ] }
    └── StreamExchange Hash([0]) from 5

    Fragment 5
    StreamTableScan { table: t2, columns: [t2.a, t2._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t2._row_id], pk: [_row_id], dist: UpstreamHashShard(t2._row_id) }
    ├── tables: [ StreamScan: 15 ]
    ├── Upstream
    └── BatchPlanNode

    Table 0 { columns: [ t1_b, count, _rw_timestamp ], primary key: [ $0 ASC ], value indices: [ 1 ], distribution key: [ 0 ], read pk prefix len hint: 1 }

    Table 1
    ├── columns: [ t1_b, t1_a, t1__row_id, t2_a, t2__row_id, _rw_timestamp ]
    ├── primary key: [ $0 ASC, $1 ASC, $2 ASC, $4 ASC ]
    ├── value indices: [ 0, 1, 2, 3, 4 ]
    ├── distribution key: [ 0 ]
    └── read pk prefix len hint: 0

    Table 2
    ├── columns: [ vnode, t1_b, t1_a, t1__row_id, t2__row_id, backfill_finished, row_count, _rw_timestamp ]
    ├── primary key: [ $0 ASC ]
    ├── value indices: [ 0, 1, 2, 3, 4, 5, 6 ]
    ├── distribution key: [ 0 ]
    ├── read pk prefix len hint: 1
    └── vnode column idx: 0

    Table 3
    ├── columns: [ t1_b, t1_a, t1__row_id, t2_a, t2__row_id, _rw_gen, _rw_op, _rw_seq, _rw_timestamp ]
    ├── primary key: [ $5 ASC, $0 ASC, $1 ASC, $2 ASC, $4 ASC, $7 ASC ]
    ├── value indices: [ 0, 1, 2, 3, 4, 5, 6, 7 ]
    ├── distribution key: [ 0 ]
    ├── read pk prefix len hint: 0
    └── clean watermark indices: [5]

    Table 4 { columns: [ t1_a, t1_b, t1__row_id, _rw_timestamp ], primary key: [ $0 ASC, $2 ASC ], value indices: [ 0, 1, 2 ], distribution key: [ 0 ], read pk prefix len hint: 1 }

    Table 5 { columns: [ t1_a, t1__row_id, _degree, _rw_timestamp ], primary key: [ $0 ASC, $1 ASC ], value indices: [ 2 ], distribution key: [ 0 ], read pk prefix len hint: 1 }

    Table 6 { columns: [ t2_a, t2__row_id, _rw_timestamp ], primary key: [ $0 ASC, $1 ASC ], value indices: [ 0, 1 ], distribution key: [ 0 ], read pk prefix len hint: 1 }

    Table 7 { columns: [ t2_a, t2__row_id, _degree, _rw_timestamp ], primary key: [ $0 ASC, $1 ASC ], value indices: [ 2 ], distribution key: [ 0 ], read pk prefix len hint: 1 }

    Table 8 { columns: [ t1_a, t1_b, t1__row_id, _rw_timestamp ], primary key: [ $0 ASC, $2 ASC ], value indices: [ 0, 1, 2 ], distribution key: [ 0 ], read pk prefix len hint: 0 }

    Table 9
    ├── columns: [ vnode, t1_a, t1__row_id, backfill_finished, row_count, _rw_timestamp ]
    ├── primary key: [ $0 ASC ]
    ├── value indices: [ 0, 1, 2, 3, 4 ]
    ├── distribution key: [ 0 ]
    ├── read pk prefix len hint: 1
    └── vnode column idx: 0

    Table 10
    ├── columns: [ t1_a, t1_b, t1__row_id, _rw_gen, _rw_op, _rw_seq, _rw_timestamp ]
    ├── primary key: [ $3 ASC, $0 ASC, $2 ASC, $5 ASC ]
    ├── value indices: [ 0, 1, 2, 3, 4, 5 ]
    ├── distribution key: [ 0 ]
    ├── read pk prefix len hint: 0
    └── clean watermark indices: [3]

    Table 11
    ├── columns: [ vnode, epoch, row_count, is_epoch_finished, _row_id, _rw_timestamp ]
    ├── primary key: [ $0 ASC ]
    ├── value indices: [ 1, 2, 3, 4 ]
    ├── distribution key: [ 0 ]
    ├── read pk prefix len hint: 1
    └── vnode column idx: 0

    Table 12 { columns: [ t2_a, t2__row_id, _rw_timestamp ], primary key: [ $0 ASC, $1 ASC ], value indices: [ 0, 1 ], distribution key: [ 0 ], read pk prefix len hint: 0 }

    Table 13
    ├── columns: [ vnode, t2_a, t2__row_id, backfill_finished, row_count, _rw_timestamp ]
    ├── primary key: [ $0 ASC ]
    ├── value indices: [ 0, 1, 2, 3, 4 ]
    ├── distribution key: [ 0 ]
    ├── read pk prefix len hint: 1
    └── vnode column idx: 0

    Table 14
    ├── columns: [ t2_a, t2__row_id, _rw_gen, _rw_op, _rw_seq, _rw_timestamp ]
    ├── primary key: [ $2 ASC, $0 ASC, $1 ASC, $4 ASC ]
    ├── value indices: [ 0, 1, 2, 3, 4 ]
    ├── distribution key: [ 0 ]
    ├── read pk prefix len hint: 0
    └── clean watermark indices: [2]

    Table 15
    ├── columns: [ vnode, epoch, row_count, is_epoch_finished, _row_id, _rw_timestamp ]
    ├── primary key: [ $0 ASC ]
    ├── value indices: [ 1, 2, 3, 4 ]
    ├── distribution key: [ 0 ]
    ├── read pk prefix len hint: 1
    └── vnode column idx: 0

    Table 4294967294 { columns: [ count, t1.b, _rw_timestamp ], primary key: [ $1 ASC ], value indices: [ 0, 1 ], distribution key: [ 1 ], read pk prefix len hint: 1 }

