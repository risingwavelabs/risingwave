# This file is automatically generated. See `src/frontend/planner_test/README.md` for more information.
- sql: |
    create table t (v1 jsonb);
    select jsonb_typeof((v1)->'x') from t;
  logical_plan: |-
    LogicalProject { exprs: [JsonbTypeof(JsonbAccessInner(t.v1, 'x':Varchar)) as $expr1] }
    └─LogicalScan { table: t, columns: [t.v1, t._row_id] }
- sql: |
    select array_transform(array[1,2,3], (x) -> x * 2)
  binder_error: |-
    Bind error: failed to bind expression: array_transform(ARRAY[1, 2, 3], (x) -> x * 2)

    Caused by:
      Item not found: Invalid column: x
