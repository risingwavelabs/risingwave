# This file is automatically generated. See `src/frontend/planner_test/README.md` for more information.
- name: MATCH_RECOGNIZE plans end-to-end to a WatermarkSort plus MatchRecognize pipeline
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures b.v as dip
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  logical_plan: |-
    LogicalProject { exprs: [partition_0, dip] }
    └─LogicalMatchRecognize { partition_by: [t.id], order_by: [t.ts], measures: ["dip"], pattern: a b }
      └─LogicalScan { table: t, columns: [t.id, t.ts, t.v, t._row_id, t._rw_timestamp] }
  stream_plan: |-
    StreamMaterialize { columns: [partition_0, dip, _match_id(hidden)], stream_key: [partition_0, _match_id], pk_columns: [partition_0, _match_id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [partition_0, dip, _match_id] }
      └─StreamMatchRecognize { partition_by: [t.id], order_by: [t.ts], measures: ["dip"], pattern: a b }
        └─StreamWatermarkSort { sort_column: t.ts }
          └─StreamExchange { dist: HashShard(t.id) }
            └─StreamFilter { predicate: IsNotNull(t.ts) }
              └─StreamTableScan { table: t, columns: [t.id, t.ts, t.v, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- name: MATCH_RECOGNIZE with a CLASSIFIER() measure
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures classifier() as cls, b.v as dip
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  logical_plan: |-
    LogicalProject { exprs: [partition_0, cls, dip] }
    └─LogicalMatchRecognize { partition_by: [t.id], order_by: [t.ts], measures: ["cls", "dip"], pattern: a b }
      └─LogicalScan { table: t, columns: [t.id, t.ts, t.v, t._row_id, t._rw_timestamp] }
  stream_plan: |-
    StreamMaterialize { columns: [partition_0, cls, dip, _match_id(hidden)], stream_key: [partition_0, _match_id], pk_columns: [partition_0, _match_id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [partition_0, cls, dip, _match_id] }
      └─StreamMatchRecognize { partition_by: [t.id], order_by: [t.ts], measures: ["cls", "dip"], pattern: a b }
        └─StreamWatermarkSort { sort_column: t.ts }
          └─StreamExchange { dist: HashShard(t.id) }
            └─StreamFilter { predicate: IsNotNull(t.ts) }
              └─StreamTableScan { table: t, columns: [t.id, t.ts, t.v, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- name: MATCH_RECOGNIZE with aggregate MEASURES over matched rows
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures count(*) as n, min(a.v) as lo, max(a.v) as hi
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  logical_plan: |-
    LogicalProject { exprs: [partition_0, n, lo, hi] }
    └─LogicalMatchRecognize { partition_by: [t.id], order_by: [t.ts], measures: ["n", "lo", "hi"], pattern: a b }
      └─LogicalScan { table: t, columns: [t.id, t.ts, t.v, t._row_id, t._rw_timestamp] }
  stream_plan: |-
    StreamMaterialize { columns: [partition_0, n, lo, hi, _match_id(hidden)], stream_key: [partition_0, _match_id], pk_columns: [partition_0, _match_id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [partition_0, n, lo, hi, _match_id] }
      └─StreamMatchRecognize { partition_by: [t.id], order_by: [t.ts], measures: ["n", "lo", "hi"], pattern: a b }
        └─StreamWatermarkSort { sort_column: t.ts }
          └─StreamExchange { dist: HashShard(t.id) }
            └─StreamFilter { predicate: IsNotNull(t.ts) }
              └─StreamTableScan { table: t, columns: [t.id, t.ts, t.v, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- name: MATCH_RECOGNIZE stream planning requires a watermark on the leading ORDER BY column
  sql: |
    create table t(id int, ts int, v int) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures b.v as dip
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  stream_error: |-
    Not supported: MATCH_RECOGNIZE without a watermark on the leading ORDER BY column
    HINT: declare one on the source or table, e.g. WATERMARK FOR ts AS ts - INTERVAL '5' SECOND
- name: MATCH_RECOGNIZE WITHIN accepts a constant bound
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures b.v as dip
      one row per match
      after match skip past last row
      pattern (a b) within 3
      define a as a.v > 0, b as b.v < 0
    );
  stream_plan: |-
    StreamMaterialize { columns: [partition_0, dip, _match_id(hidden)], stream_key: [partition_0, _match_id], pk_columns: [partition_0, _match_id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [partition_0, dip, _match_id] }
      └─StreamMatchRecognize { partition_by: [t.id], order_by: [t.ts], measures: ["dip"], pattern: a b }
        └─StreamWatermarkSort { sort_column: t.ts }
          └─StreamExchange { dist: HashShard(t.id) }
            └─StreamFilter { predicate: IsNotNull(t.ts) }
              └─StreamTableScan { table: t, columns: [t.id, t.ts, t.v, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- name: MATCH_RECOGNIZE WITHIN rejects an input column reference (would evaluate against a synthetic order-key row and crash)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures b.v as dip
      one row per match
      after match skip past last row
      pattern (a b) within v
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Feature is not yet implemented: MATCH_RECOGNIZE WITHIN bound must be a constant expression; input column references are not supported
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE WITHIN rejects a non-constant expression over an input column
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures b.v as dip
      one row per match
      after match skip past last row
      pattern (a b) within v + 1
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Feature is not yet implemented: MATCH_RECOGNIZE WITHIN bound must be a constant expression; input column references are not supported
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE rejects a non-boolean DEFINE predicate at bind time
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures b.v as dip
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v, b as b.v < 0
    );
  binder_error: argument of the DEFINE predicate of a must be boolean, not type Int32
- name: MATCH_RECOGNIZE navigation over a duplicated input column name is ambiguous
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from (select id, ts, v as x, v + 100 as x from t) match_recognize (
      partition by id
      order by ts
      measures b.ts as dip
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.ts > 0, b as prev(x) > b.ts
    );
  binder_error: 'Bind error: column reference "x" in MATCH_RECOGNIZE navigation is ambiguous: the input has more than one column with that name'
- name: MATCH_RECOGNIZE MEASURES aggregates go through the regular signatures (no max(boolean))
  sql: |
    create table t(id int, ts int, flag boolean, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures max(a.flag) as m
      one row per match
      after match skip past last row
      pattern (a+)
      define a as a.flag
    );
  binder_error: function max(boolean) does not exist
- name: MATCH_RECOGNIZE reserves the hidden _match_id name against measure aliases
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as _match_id
      one row per match
      after match skip past last row
      pattern (a)
      define a as a.v > 0
    );
  binder_error: 'Bind error: the measure alias `_match_id` collides with the hidden per-match id column MATCH_RECOGNIZE appends; choose another alias'
- name: MATCH_RECOGNIZE MEASURES rejects a subquery (no representation in the executor's scalar expressions)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures (select max(v) from t) as x
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Not supported: a subquery in the MEASURES item `x`
    HINT: a measure must be a scalar expression over the pattern variables
- name: MATCH_RECOGNIZE MEASURES rejects COUNT(DISTINCT ...) (would silently evaluate as plain COUNT)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures count(distinct a.v) as n
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Feature is not yet implemented: DISTINCT on COUNT() in MATCH_RECOGNIZE MEASURES
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE MEASURES rejects an aggregate FILTER clause (would silently evaluate unfiltered)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures sum(a.v) filter (where a.v > 10) as s
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Feature is not yet implemented: FILTER on SUM() in MATCH_RECOGNIZE MEASURES
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE MEASURES rejects an aggregate ORDER BY clause
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures min(a.v order by a.v) as lo
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Feature is not yet implemented: ORDER BY on MIN() in MATCH_RECOGNIZE MEASURES
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE MEASURES rejects an OVER clause on an aggregate
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures max(a.v) over () as hi
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Feature is not yet implemented: OVER on MAX() in MATCH_RECOGNIZE MEASURES
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE MEASURES rejects WITHIN GROUP on an aggregate
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures count(a.v) within group (order by a.v) as n
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Feature is not yet implemented: WITHIN GROUP on COUNT() in MATCH_RECOGNIZE MEASURES
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE MEASURES rejects FILTER on FIRST/LAST navigation
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures first(a.v) filter (where a.v > 0) as f
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Feature is not yet implemented: FILTER on FIRST() in MATCH_RECOGNIZE MEASURES
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE MEASURES rejects an OVER clause on CLASSIFIER()
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures classifier() over () as cls
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Feature is not yet implemented: OVER on CLASSIFIER() in MATCH_RECOGNIZE MEASURES
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE rejects ALL ROWS PER MATCH (not yet supported)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures b.v as dip
      all rows per match
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Feature is not yet implemented: ALL ROWS PER MATCH
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE rejects a range quantifier whose lower bound exceeds its upper bound (silently became {5})
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a{5,3} b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Not supported: a range quantifier with a lower bound above its upper bound ({5,3})
    HINT: use {3,5} if the bounds were swapped, or {5} for exactly 5 repetitions
- name: MATCH_RECOGNIZE rejects a range quantifier above the expansion cap (the NFA is expanded eagerly at actor build time)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a{2000} b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Not supported: a range quantifier bound of 2000
    HINT: a bound is expanded eagerly into up to 4 NFA states per repetition of a single variable (more for a larger inner pattern), so it may be at most 1000
- name: MATCH_RECOGNIZE rejects an at-least quantifier above the expansion cap
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a{2000,} b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Not supported: a range quantifier bound of 2000
    HINT: a bound is expanded eagerly into up to 4 NFA states per repetition of a single variable (more for a larger inner pattern), so it may be at most 1000
- name: MATCH_RECOGNIZE rejects nested quantifiers whose product exceeds the total NFA state budget
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern ((a{900}){900} b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Not supported: a MATCH_RECOGNIZE pattern that expands to about 1620002 NFA states
    HINT: the pattern is expanded eagerly into at most 100000 states; reduce the quantifier bounds, especially where quantifiers are nested
- name: MATCH_RECOGNIZE DEFINE rejects extra arguments to PREV (they were silently ignored)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < prev(b.v, 1, 999)
    );
  binder_error: |-
    Not supported: PREV() with 3 arguments in DEFINE
    HINT: PREV() takes a column and an optional positive integer offset
- name: MATCH_RECOGNIZE DEFINE rejects a PREV offset above u32 (it truncated to 0, i.e. the candidate row)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < prev(b.v, 4294967296)
    );
  binder_error: |-
    Not supported: PREV() offset of 4294967296
    HINT: a PREV() offset requires that many mandatory pattern rows before the variable, so the offset may be at most 100
- name: MATCH_RECOGNIZE DEFINE rejects FILTER on navigation (it was silently dropped)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < last(a.v) filter (where a.v > 0)
    );
  binder_error: |-
    Feature is not yet implemented: FILTER on LAST() in MATCH_RECOGNIZE DEFINE
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE DEFINE rejects an OVER clause on navigation (it was silently dropped)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < last(a.v) over ()
    );
  binder_error: |-
    Feature is not yet implemented: OVER on LAST() in MATCH_RECOGNIZE DEFINE
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE DEFINE rejects DISTINCT on navigation (it was silently dropped)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < last(distinct a.v)
    );
  binder_error: |-
    Feature is not yet implemented: DISTINCT on LAST() in MATCH_RECOGNIZE DEFINE
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE DEFINE rejects IGNORE NULLS on navigation (it was silently dropped)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < prev(b.v, 1 ignore nulls)
    );
  binder_error: |-
    Feature is not yet implemented: IGNORE NULLS on PREV() in MATCH_RECOGNIZE DEFINE
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE rejects AFTER MATCH SKIP TO a variable absent from PATTERN (it degraded on every match)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip to last x
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Feature is not yet implemented: AFTER MATCH SKIP TO FIRST/LAST references unknown pattern variable x
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE DEFINE navigation nested in a CASE expression
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < case when prev(b.v) > 0 then prev(b.v) else 0 end
    );
  stream_plan: |-
    StreamMaterialize { columns: [partition_0, m, _match_id(hidden)], stream_key: [partition_0, _match_id], pk_columns: [partition_0, _match_id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [partition_0, m, _match_id] }
      └─StreamMatchRecognize { partition_by: [t.id], order_by: [t.ts], measures: ["m"], pattern: a b }
        └─StreamWatermarkSort { sort_column: t.ts }
          └─StreamExchange { dist: HashShard(t.id) }
            └─StreamFilter { predicate: IsNotNull(t.ts) }
              └─StreamTableScan { table: t, columns: [t.id, t.ts, t.v, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- name: MATCH_RECOGNIZE DEFINE navigation nested in an IN list
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v in (prev(b.v), last(a.v))
    );
  stream_plan: |-
    StreamMaterialize { columns: [partition_0, m, _match_id(hidden)], stream_key: [partition_0, _match_id], pk_columns: [partition_0, _match_id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [partition_0, m, _match_id] }
      └─StreamMatchRecognize { partition_by: [t.id], order_by: [t.ts], measures: ["m"], pattern: a b }
        └─StreamWatermarkSort { sort_column: t.ts }
          └─StreamExchange { dist: HashShard(t.id) }
            └─StreamFilter { predicate: IsNotNull(t.ts) }
              └─StreamTableScan { table: t, columns: [t.id, t.ts, t.v, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- name: MATCH_RECOGNIZE rejects PERMUTE over too many variables by its arity, not by the state budget
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (permute(a, b, c, d, e, f, g, h))
      define a as a.v > 0, b as b.v < 0, c as c.v > 1, d as d.v > 2,
             e as e.v > 3, f as f.v > 4, g as g.v > 5, h as h.v > 6
    );
  binder_error: |-
    Not supported: PERMUTE over 8 variables (expands to 8! orderings)
    HINT: PERMUTE supports at most 6 variables
- name: MATCH_RECOGNIZE DEFINE navigation on the left of IN (subquery) is extracted, not reported as an unknown function
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as prev(b.v) in (select v from t)
    );
  binder_error: |-
    Not supported: a subquery in the DEFINE predicate of b
    HINT: a DEFINE predicate must be a scalar expression over the pattern variables
- name: MATCH_RECOGNIZE DEFINE navigation nested in IS DISTINCT FROM, LIKE, an array constructor and a subscript (traversal reaches the PREV(b.v, 2) that reads before the match, so the start-distance rule rejects it)
  sql: |
    create table t(id int, ts int, v int, s varchar, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0,
             b as (prev(b.v) is distinct from b.v)
                  and (prev(s) like 'a%')
                  and ((array[prev(b.v, 2), last(a.v)])[1] < b.v)
    );
  binder_error: |-
    Not supported: PREV with offset 2 in DEFINE b: the variable can occur 1 row(s) from the match start, so the read could reach rows before the match, which are not retained
    HINT: ensure the variable is always preceded by at least as many pattern rows as the PREV offset, e.g. prefix the pattern with an anchor variable (`x` with `x AS TRUE`)
- name: MATCH_RECOGNIZE DEFINE rejects a PREV offset above the operational cap (the offset demands that many mandatory pattern rows)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < prev(b.v, 101)
    );
  binder_error: |-
    Not supported: PREV() offset of 101
    HINT: a PREV() offset requires that many mandatory pattern rows before the variable, so the offset may be at most 100
- name: MATCH_RECOGNIZE DEFINE rejects PREV on a variable that can start the match (the read would reach before the match)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a b)
      define a as prev(a.v) is null, b as b.v < 0
    );
  binder_error: |-
    Not supported: PREV with offset 1 in DEFINE a: the variable can occur 0 row(s) from the match start, so the read could reach rows before the match, which are not retained
    HINT: ensure the variable is always preceded by at least as many pattern rows as the PREV offset, e.g. prefix the pattern with an anchor variable (`x` with `x AS TRUE`)
- name: MATCH_RECOGNIZE DEFINE accepts PREV on a variable always preceded by enough rows (the anchored V-shape)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a b+)
      define b as b.v < prev(b.v)
    );
  stream_plan: |-
    StreamMaterialize { columns: [partition_0, m, _match_id(hidden)], stream_key: [partition_0, _match_id], pk_columns: [partition_0, _match_id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [partition_0, m, _match_id] }
      └─StreamMatchRecognize { partition_by: [t.id], order_by: [t.ts], measures: ["m"], pattern: a b+ }
        └─StreamWatermarkSort { sort_column: t.ts }
          └─StreamExchange { dist: HashShard(t.id) }
            └─StreamFilter { predicate: IsNotNull(t.ts) }
              └─StreamTableScan { table: t, columns: [t.id, t.ts, t.v, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- name: MATCH_RECOGNIZE DEFINE rejects PREV behind a zero-minimum quantifier prefix (the prefix can match no rows, so the variable can start the match)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a* b)
      define a as a.v > 0, b as b.v < prev(b.v)
    );
  binder_error: |-
    Not supported: PREV with offset 1 in DEFINE b: the variable can occur 0 row(s) from the match start, so the read could reach rows before the match, which are not retained
    HINT: ensure the variable is always preceded by at least as many pattern rows as the PREV offset, e.g. prefix the pattern with an anchor variable (`x` with `x AS TRUE`)
- name: MATCH_RECOGNIZE DEFINE accepts PREV behind a one-minimum quantifier prefix
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a+ b)
      define a as a.v > 0, b as b.v < prev(b.v)
    );
  stream_plan: |-
    StreamMaterialize { columns: [partition_0, m, _match_id(hidden)], stream_key: [partition_0, _match_id], pk_columns: [partition_0, _match_id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [partition_0, m, _match_id] }
      └─StreamMatchRecognize { partition_by: [t.id], order_by: [t.ts], measures: ["m"], pattern: a+ b }
        └─StreamWatermarkSort { sort_column: t.ts }
          └─StreamExchange { dist: HashShard(t.id) }
            └─StreamFilter { predicate: IsNotNull(t.ts) }
              └─StreamTableScan { table: t, columns: [t.id, t.ts, t.v, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- name: MATCH_RECOGNIZE DEFINE rejects PREV inside PERMUTE (any variable can be ordered first)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (permute(a, b))
      define a as a.v > 0, b as b.v < prev(b.v)
    );
  binder_error: |-
    Not supported: PREV with offset 1 in DEFINE b: the variable can occur 0 row(s) from the match start, so the read could reach rows before the match, which are not retained
    HINT: ensure the variable is always preceded by at least as many pattern rows as the PREV offset, e.g. prefix the pattern with an anchor variable (`x` with `x AS TRUE`)
- name: MATCH_RECOGNIZE DEFINE accepts PREV whose distance is met through an alternation (both branches consume a row first)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (s (a | b) c)
      define s as s.v > 0, a as a.v > 0, b as b.v < 0, c as c.v < prev(c.v, 2)
    );
  stream_plan: |-
    StreamMaterialize { columns: [partition_0, m, _match_id(hidden)], stream_key: [partition_0, _match_id], pk_columns: [partition_0, _match_id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [partition_0, m, _match_id] }
      └─StreamMatchRecognize { partition_by: [t.id], order_by: [t.ts], measures: ["m"], pattern: s (a | b) c }
        └─StreamWatermarkSort { sort_column: t.ts }
          └─StreamExchange { dist: HashShard(t.id) }
            └─StreamFilter { predicate: IsNotNull(t.ts) }
              └─StreamTableScan { table: t, columns: [t.id, t.ts, t.v, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- name: MATCH_RECOGNIZE DEFINE rejects physical NEXT (a global decision horizon can starve an idle partition; per-path decidability is future work)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures a.v as m
      one row per match
      after match skip past last row
      pattern (a b)
      define a as next(a.v) < 0, b as b.v < 0
    );
  binder_error: |-
    Feature is not yet implemented: physical NEXT() in MATCH_RECOGNIZE DEFINE (a row's verdict would depend on rows after it; per-candidate decidability is not implemented yet)
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE WITHIN rejects a non-positive bound (nothing could ever match; the MV would be silently empty forever)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures b.v as dip
      one row per match
      after match skip past last row
      pattern (a b) within -1
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Not supported: a MATCH_RECOGNIZE WITHIN bound that is NULL, zero or negative, or an interval with a negative component
    HINT: the bound is the maximum span of a match; a non-positive bound can never hold a multi-row match (the view would stay empty) and a NULL bound bounds nothing — use a positive constant interval whose months, days and seconds are all non-negative
- name: MATCH_RECOGNIZE WITHIN rejects a zero bound
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures b.v as dip
      one row per match
      after match skip past last row
      pattern (a b) within 0
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Not supported: a MATCH_RECOGNIZE WITHIN bound that is NULL, zero or negative, or an interval with a negative component
    HINT: the bound is the maximum span of a match; a non-positive bound can never hold a multi-row match (the view would stay empty) and a NULL bound bounds nothing — use a positive constant interval whose months, days and seconds are all non-negative
- name: MATCH_RECOGNIZE WITHIN rejects a NULL bound (the span predicate would silently never hold rows to a window)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures b.v as dip
      one row per match
      after match skip past last row
      pattern (a b) within null
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Not supported: a MATCH_RECOGNIZE WITHIN bound that is NULL, zero or negative, or an interval with a negative component
    HINT: the bound is the maximum span of a match; a non-positive bound can never hold a multi-row match (the view would stay empty) and a NULL bound bounds nothing — use a positive constant interval whose months, days and seconds are all non-negative
- name: MATCH_RECOGNIZE MEASURES rejects physical PREV/NEXT (navigation over rows outside the match; not the misleading "function does not exist")
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures prev(a.v) as before_first
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Feature is not yet implemented: physical PREV() in MATCH_RECOGNIZE MEASURES (it reads rows outside the match; use FIRST/LAST over a pattern variable, or PREV in DEFINE)
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE zero-repetition quantifier collapses to its suffix (a{0} b matches like plain b)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures b.v as dip
      one row per match
      after match skip past last row
      pattern (a{0} b)
      define a as a.v > 0, b as b.v < 0
    );
  stream_plan: |-
    StreamMaterialize { columns: [partition_0, dip, _match_id(hidden)], stream_key: [partition_0, _match_id], pk_columns: [partition_0, _match_id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [partition_0, dip, _match_id] }
      └─StreamMatchRecognize { partition_by: [t.id], order_by: [t.ts], measures: ["dip"], pattern: a{0} b }
        └─StreamWatermarkSort { sort_column: t.ts }
          └─StreamExchange { dist: HashShard(t.id) }
            └─StreamFilter { predicate: IsNotNull(t.ts) }
              └─StreamTableScan { table: t, columns: [t.id, t.ts, t.v, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- name: multi-column PARTITION BY in non-ascending column order exchanges by the EXACT key order (the matcher's state table hashes its distribution key in PARTITION BY order, so a subset- or ascending-order exchange would route rows to vnodes the table does not compute)
  sql: |
    create table t(id int, cat varchar, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by cat, id
      order by ts
      measures b.v as dip
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  stream_plan: |-
    StreamMaterialize { columns: [partition_0, partition_1, dip, _match_id(hidden)], stream_key: [partition_0, partition_1, _match_id], pk_columns: [partition_0, partition_1, _match_id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [partition_0, partition_1, dip, _match_id] }
      └─StreamMatchRecognize { partition_by: [t.cat, t.id], order_by: [t.ts], measures: ["dip"], pattern: a b }
        └─StreamWatermarkSort { sort_column: t.ts }
          └─StreamExchange { dist: HashShard(t.cat, t.id) }
            └─StreamFilter { predicate: IsNotNull(t.ts) }
              └─StreamTableScan { table: t, columns: [t.id, t.cat, t.ts, t.v, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- name: PERMUTE rejects duplicate variables (silently redundant NFA branches otherwise)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures count(*) as n
      one row per match
      after match skip past last row
      pattern (permute(a, a, b))
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: 'Invalid input syntax: duplicate variable `a` in PERMUTE'
- name: SUBSET alias colliding with a pattern variable is rejected (it would silently shadow)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures count(*) as n
      one row per match
      after match skip past last row
      pattern (a b)
      subset a = (b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: 'Invalid input syntax: SUBSET name `a` collides with a pattern variable or another SUBSET'
- name: duplicate SUBSET aliases are rejected
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures count(*) as n
      one row per match
      after match skip past last row
      pattern (a b)
      subset u = (a), u = (b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: 'Invalid input syntax: SUBSET name `u` collides with a pattern variable or another SUBSET'
- name: non-append-only input is rejected at planning (updates/deletes cannot revise emitted matches; a plain table cannot even declare a watermark, and the append-only check fires first)
  sql: |
    create table t(id int, ts int, v int);
    select * from t match_recognize (
      partition by id
      order by ts
      measures b.v as dip
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  stream_error: |-
    Not supported: MATCH_RECOGNIZE over a non-append-only input (updates or deletes could revise an already-emitted match)
    HINT: use an append-only source or table (e.g. CREATE TABLE ... APPEND ONLY)
- name: PARTITION BY expression gets an actionable NotSupported error, not a bare failure
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id + 1
      order by ts
      measures b.v as dip
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  stream_error: |-
    Not supported: MATCH_RECOGNIZE with an expression in PARTITION BY or ORDER BY
    HINT: use plain column references; compute the expression in a view below and partition/order by the resulting column
- name: multi-column ORDER BY shows the secondary order the sort enforces (EXPLAIN visibility)
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts, v
      measures b.v as dip
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  stream_plan: |-
    StreamMaterialize { columns: [partition_0, dip, _match_id(hidden)], stream_key: [partition_0, _match_id], pk_columns: [partition_0, _match_id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [partition_0, dip, _match_id] }
      └─StreamMatchRecognize { partition_by: [t.id], order_by: [t.ts, t.v], measures: ["dip"], pattern: a b }
        └─StreamWatermarkSort { sort_column: t.ts, secondary_order_columns: [t.v] }
          └─StreamExchange { dist: HashShard(t.id) }
            └─StreamFilter { predicate: IsNotNull(t.ts) }
              └─StreamTableScan { table: t, columns: [t.id, t.ts, t.v, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- name: SUBSET referencing an unknown pattern variable is a user error, not a missing feature
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures count(*) as n
      one row per match
      after match skip past last row
      pattern (a b)
      subset u = (a, typo)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: 'Invalid input syntax: SUBSET u references unknown pattern variable typo'
- name: a pattern variable defined twice is rejected, not silently last-wins
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures count(*) as n
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0, a as a.v < 0
    );
  binder_error: 'Invalid input syntax: pattern variable `a` is defined more than once in DEFINE'
- name: an aggregate nested inside a larger MEASURES expression is rejected at bind time
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures sum(a.v) + 1 as m
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0
    );
  binder_error: |-
    Feature is not yet implemented: an aggregate inside a larger MEASURES expression; aggregates are supported only as the whole measure (count/min/max/sum/avg over one pattern-variable column)
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: a correlated reference inside DEFINE is traversed, not silently dropped
  sql: |
    create table t1(x int) append only;
    create table t2(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t1 where exists (
      select 1 from t2 match_recognize (
        partition by id
        order by ts
        measures count(*) as n
        one row per match
        after match skip past last row
        pattern (a b)
        define a as a.v > t1.x, b as b.v < 0
      )
    );
  stream_error: |-
    Not supported: streaming nested-loop join
    HINT: The non-equal join in the query requires a nested-loop join executor, which could be very expensive to run. Consider rewriting the query to use dynamic filter as a substitute if possible.
    See also: https://docs.risingwave.com/processing/sql/dynamic-filters
- name: a pattern variable colliding with the input table name is a naming error, not an internal one
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures count(*) as n
      one row per match
      after match skip past last row
      pattern (t b)
      define t as t.v > 0, b as b.v < 0
    );
  binder_error: 'Invalid input syntax: pattern variable `t` collides with the name of a relation in scope (the MATCH_RECOGNIZE input table, most likely); rename the variable or give the input a different alias'
- name: a case-folded reference to a quoted pattern variable names the variables actually in scope
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures count(*) as n
      one row per match
      after match skip past last row
      pattern ("A" b)
      define "A" as A.v > 0, b as b.v < 0
    );
  binder_error: |-
    Bind error: Failed to bind expression: A.v > 0: Item not found: missing FROM-clause entry for table "a"
    while binding the DEFINE predicate of `A`; pattern variables in scope (case-sensitive as written): A, b
- name: MATCH_RECOGNIZE rejects a DEFINE for a symbol that does not appear in PATTERN
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures count(*) as n
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < 0, ghost as ghost.v = 0
    );
  binder_error: 'Invalid input syntax: DEFINE names `ghost`, which does not appear in PATTERN; its predicate could never be evaluated'
- name: MATCH_RECOGNIZE DEFINE rejects PREV over an undeclared variable qualifier
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures count(*) as n
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < prev(zzz.v)
    );
  binder_error: 'Invalid input syntax: PREV/NEXT in DEFINE references unknown pattern variable `zzz`'
- name: MATCH_RECOGNIZE DEFINE rejects PREV anchored to another declared variable
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures count(*) as n
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < prev(a.v)
    );
  binder_error: |-
    Feature is not yet implemented: PREV/NEXT anchored to another pattern variable's rows in DEFINE (`a` inside the definition of `b`); qualify with the symbol being defined, or leave the column unqualified, for physical navigation from the current row
    No tracking issue yet. Feel free to submit a feature request at https://github.com/risingwavelabs/risingwave/issues/new?labels=type%2Ffeature&template=feature_request.yml
- name: MATCH_RECOGNIZE DEFINE allows PREV qualified by the symbol being defined
  sql: |
    create table t(id int, ts int, v int, watermark for ts as ts - 10) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures count(*) as n
      one row per match
      after match skip past last row
      pattern (a b)
      define a as a.v > 0, b as b.v < prev(b.v)
    );
  logical_plan: |-
    LogicalProject { exprs: [partition_0, n] }
    └─LogicalMatchRecognize { partition_by: [t.id], order_by: [t.ts], measures: ["n"], pattern: a b }
      └─LogicalScan { table: t, columns: [t.id, t.ts, t.v, t._row_id, t._rw_timestamp] }
  stream_plan: |-
    StreamMaterialize { columns: [partition_0, n, _match_id(hidden)], stream_key: [partition_0, _match_id], pk_columns: [partition_0, _match_id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [partition_0, n, _match_id] }
      └─StreamMatchRecognize { partition_by: [t.id], order_by: [t.ts], measures: ["n"], pattern: a b }
        └─StreamWatermarkSort { sort_column: t.ts }
          └─StreamExchange { dist: HashShard(t.id) }
            └─StreamFilter { predicate: IsNotNull(t.ts) }
              └─StreamTableScan { table: t, columns: [t.id, t.ts, t.v, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
- name: MATCH_RECOGNIZE plans with a timestamp ORDER BY key and an interval WITHIN bound
  sql: |
    create table t(id int, ts timestamp, v int, watermark for ts as ts - interval '10' second) append only;
    select * from t match_recognize (
      partition by id
      order by ts
      measures count(*) as n
      one row per match
      after match skip past last row
      pattern (a b) within interval '5' minute
      define a as a.v > 0, b as b.v < 0
    );
  logical_plan: |-
    LogicalProject { exprs: [partition_0, n] }
    └─LogicalMatchRecognize { partition_by: [t.id], order_by: [t.ts], measures: ["n"], pattern: a b }
      └─LogicalScan { table: t, columns: [t.id, t.ts, t.v, t._row_id, t._rw_timestamp] }
  stream_plan: |-
    StreamMaterialize { columns: [partition_0, n, _match_id(hidden)], stream_key: [partition_0, _match_id], pk_columns: [partition_0, _match_id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [partition_0, n, _match_id] }
      └─StreamMatchRecognize { partition_by: [t.id], order_by: [t.ts], measures: ["n"], pattern: a b }
        └─StreamWatermarkSort { sort_column: t.ts }
          └─StreamExchange { dist: HashShard(t.id) }
            └─StreamFilter { predicate: IsNotNull(t.ts) }
              └─StreamTableScan { table: t, columns: [t.id, t.ts, t.v, t._row_id], stream_scan_type: SnapshotBackfill, stream_key: [t._row_id], pk: [_row_id], dist: UpstreamHashShard(t._row_id) }
