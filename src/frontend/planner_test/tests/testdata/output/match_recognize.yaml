# This file is automatically generated. See `src/frontend/planner_test/README.md` for more information.
- name: MATCH_RECOGNIZE binds; planning is not yet implemented
  sql: |
    create table t(id int, ts timestamp, v int);
    select * from t match_recognize (
      partition by id
      order by ts
      measures count(*) as cnt
      one row per match
      after match skip past last row
      pattern (a b+ c?)
      define a as a.v > 0, b as b.v < 0, c as c.v = 0
    );
  logical_plan: |-
    LogicalProject { exprs: [partition_0, cnt] }
    └─LogicalMatchRecognize { partition_by: [$0], order_by: [$1], measures: ["cnt"], pattern: a b+ c? }
      └─LogicalScan { table: t, columns: [t.id, t.ts, t.v, t._row_id, t._rw_timestamp] }
  stream_error: StreamMatchRecognize is not implemented yet
