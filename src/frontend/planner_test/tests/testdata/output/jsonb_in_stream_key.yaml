# This file is automatically generated. See `src/frontend/planner_test/README.md` for more information.
- name: jsonb in group by
  sql: |
    create table t1 (v1 jsonb, v2 int);
    create table t2 (v3 jsonb, v4 int);
    select v2 from t1 group by v2, v1;
  stream_error: |-
    Not supported: JSONB column "t1.v1" should not be in the aggregation group key.
    HINT: Using JSONB columns as part of the join or aggregation keys can severely impair performance. If you intend to proceed, force to enable it with: `set rw_streaming_allow_jsonb_in_stream_key to true`
- name: jsonb in union
  sql: |
    create table t1 (v1 jsonb, v2 int);
    create table t2 (v3 jsonb, v4 int);
    select v1, v2 from t1 union select v3, v4 from t2;
  stream_error: |-
    Not supported: JSONB column "t1.v1" should not be in the field.
    HINT: Using JSONB columns as part of the join or aggregation keys can severely impair performance. If you intend to proceed, force to enable it with: `set rw_streaming_allow_jsonb_in_stream_key to true`
- name: jsonb in distinct
  sql: |
    create table t1 (v1 jsonb, v2 int);
    select distinct v1 from t1;
  stream_error: |-
    Not supported: JSONB column "t1.v1" should not be in the aggregation group key.
    HINT: Using JSONB columns as part of the join or aggregation keys can severely impair performance. If you intend to proceed, force to enable it with: `set rw_streaming_allow_jsonb_in_stream_key to true`
- name: jsonb in TopN by group
  sql: |
    create table t1 (v1 jsonb, v2 int);
    SELECT v1 FROM (
      SELECT v1, rank() OVER (PARTITION BY v1 ORDER BY v2) AS rank
      FROM t1)
    WHERE rank <= 2;
  stream_error: |-
    Not supported: JSONB column "t1.v1" should not be in the over window partition key.
    HINT: Using JSONB columns as part of the join or aggregation keys can severely impair performance. If you intend to proceed, force to enable it with: `set rw_streaming_allow_jsonb_in_stream_key to true`
- name: variant in group by with jsonb opt-in
  sql: |
    set rw_streaming_allow_jsonb_in_stream_key = true;
    create table t1 (v1 variant, v2 int);
    select v2 from t1 group by v2, v1;
  stream_error: |-
    Not supported: VARIANT column "t1.v1" should not be in the aggregation group key.
    HINT: VARIANT values only have byte-wise equality and ordering, so they cannot be used as a key: in a primary key, index, grouping or join key, ORDER BY, or set operation.
- name: variant in materialized view order by
  sql: |
    create table t1 (v1 variant, v2 int);
    select v1, v2 from t1 order by v1;
  stream_error: |-
    Not supported: VARIANT column "v1" is part of the storage primary key
    HINT: VARIANT values only have byte-wise equality and ordering, so they cannot be used as a key: in a primary key, index, grouping or join key, ORDER BY, or set operation.
- name: nested variant in TopN order key
  sql: |
    create table t1 (id int primary key, s struct<v variant>);
    select count(*) from (select * from t1 order by s limit 10);
  stream_error: |-
    Not supported: VARIANT column "t1.s" should not be in the TopN order key.
    HINT: VARIANT values only have byte-wise equality and ordering, so they cannot be used as a key: in a primary key, index, grouping or join key, ORDER BY, or set operation.
- name: nested variant in join key
  sql: |
    create table t1 (id int primary key, s struct<v variant>);
    create table t2 (id int primary key, s struct<v variant>);
    select t1.id from t1 join t2 on t1.s = t2.s;
  batch_error: |-
    Not supported: VARIANT column "t1.s" should not be in the join key.
    HINT: VARIANT values only have byte-wise equality and ordering, so they cannot be used as a key: in a primary key, index, grouping or join key, ORDER BY, or set operation.
  stream_error: |-
    Not supported: VARIANT column "t1.s" should not be in the join key.
    HINT: VARIANT values only have byte-wise equality and ordering, so they cannot be used as a key: in a primary key, index, grouping or join key, ORDER BY, or set operation.
- name: nested variant in over window partition key
  sql: |
    create table t1 (id int primary key, s struct<v variant>);
    select id from (
      select id, rank() OVER (PARTITION BY s ORDER BY id) AS rank
      from t1)
    where rank <= 2;
  stream_error: |-
    Not supported: VARIANT column "t1.s" should not be in the over window partition key.
    HINT: VARIANT values only have byte-wise equality and ordering, so they cannot be used as a key: in a primary key, index, grouping or join key, ORDER BY, or set operation.
