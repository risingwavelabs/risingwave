# This file is automatically generated. See `src/frontend/planner_test/README.md` for more information.
- sql: |
    create table t (a int);
    select _rw_timestamp from t;
  logical_plan: |-
    LogicalProject { exprs: [t._rw_timestamp] }
    └─LogicalScan { table: t, columns: [t.a, t._row_id, t._rw_timestamp] }
  batch_plan: |-
    BatchExchange { order: [], dist: Single }
    └─BatchScan { table: t, columns: [t._rw_timestamp], distribution: SomeShard }
  stream_error: |-
    Not supported: selecting `_rw_timestamp` in a streaming query is not allowed
    HINT: please run the sql in batch mode or remove the column `_rw_timestamp` from the streaming query
- sql: |
    create table t (a int);
    select t.*, _rw_timestamp from t;
  logical_plan: |-
    LogicalProject { exprs: [t.a, t._rw_timestamp] }
    └─LogicalScan { table: t, columns: [t.a, t._row_id, t._rw_timestamp] }
  batch_plan: |-
    BatchExchange { order: [], dist: Single }
    └─BatchScan { table: t, columns: [t.a, t._rw_timestamp], distribution: SomeShard }
  stream_error: |-
    Not supported: selecting `_rw_timestamp` in a streaming query is not allowed
    HINT: please run the sql in batch mode or remove the column `_rw_timestamp` from the streaming query
- sql: |
    create table t (a int);
    create index idx on t(a);
    select _rw_timestamp from t where a = 1;
  logical_plan: |-
    LogicalProject { exprs: [t._rw_timestamp] }
    └─LogicalFilter { predicate: (t.a = 1:Int32) }
      └─LogicalScan { table: t, columns: [t.a, t._row_id, t._rw_timestamp] }
  batch_plan: |-
    BatchExchange { order: [], dist: Single }
    └─BatchLookupJoin { type: Inner, predicate: idx.t._row_id IS NOT DISTINCT FROM t._row_id, output: [t._rw_timestamp], lookup table: t }
      └─BatchExchange { order: [], dist: UpstreamHashShard(idx.t._row_id) }
        └─BatchScan { table: idx, columns: [idx.t._row_id], scan_ranges: [idx.a = Int32(1)], distribution: SomeShard }
  stream_error: |-
    Not supported: selecting `_rw_timestamp` in a streaming query is not allowed
    HINT: please run the sql in batch mode or remove the column `_rw_timestamp` from the streaming query
