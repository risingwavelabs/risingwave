# This file is automatically generated. See `src/frontend/planner_test/README.md` for more information.
- name: SQL UDF with FROM called twice
  sql: |
    create function g_from(j jsonb) returns varchar
    language sql as $$ select y ->> 'k1' from (select j as y) t $$;
    create table payloads (id int primary key, payload jsonb);
    select id, g_from(payload) as a, g_from(payload) as b from payloads;
  logical_plan: |-
    LogicalProject { exprs: [payloads.id, $expr2, $expr4] }
    └─LogicalApply { type: LeftOuter, on: true, correlated_id: 1, max_one_row: true }
      ├─LogicalScan { table: payloads, columns: [payloads.id, payloads.payload, payloads._rw_timestamp] }
      └─LogicalJoin { type: FullOuter, on: true, output: all }
        ├─LogicalProject { exprs: [JsonbAccessStr($expr1, 'k1':Varchar) as $expr2] }
        │ └─LogicalProject { exprs: [CorrelatedInputRef { index: 1, correlated_id: 1 } as $expr1] }
        │   └─LogicalValues { rows: [[]], schema: Schema { fields: [] } }
        └─LogicalProject { exprs: [JsonbAccessStr($expr3, 'k1':Varchar) as $expr4] }
          └─LogicalProject { exprs: [CorrelatedInputRef { index: 1, correlated_id: 1 } as $expr3] }
            └─LogicalValues { rows: [[]], schema: Schema { fields: [] } }
  optimized_logical_plan_for_stream: |-
    LogicalProject { exprs: [payloads.id, $expr1, $expr1] }
    └─LogicalProject { exprs: [payloads.id, payloads.payload, JsonbAccessStr(payloads.payload, 'k1':Varchar) as $expr1] }
      └─LogicalScan { table: payloads, columns: [payloads.id, payloads.payload] }
  stream_plan: |-
    StreamMaterialize { columns: [id, a, b], stream_key: [id], pk_columns: [id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [payloads.id, $expr1, $expr1] }
      └─StreamProject { exprs: [payloads.id, payloads.payload, JsonbAccessStr(payloads.payload, 'k1':Varchar) as $expr1], noop_update_hint: true }
        └─StreamTableScan { table: payloads, columns: [payloads.id, payloads.payload], stream_scan_type: SnapshotBackfill, stream_key: [payloads.id], pk: [id], dist: UpstreamHashShard(payloads.id) }
- name: scalar SQL UDF called twice
  sql: |
    create function g_plain(j jsonb) returns varchar
    language sql as $$ select j ->> 'k1' $$;
    create table payloads (id int primary key, payload jsonb);
    select id, g_plain(payload) as a, g_plain(payload) as b from payloads;
  stream_plan: |-
    StreamMaterialize { columns: [id, a, b], stream_key: [id], pk_columns: [id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [payloads.id, $expr1, $expr1] }
      └─StreamProject { exprs: [payloads.id, payloads.payload, JsonbAccessStr(payloads.payload, 'k1':Varchar) as $expr1], noop_update_hint: true }
        └─StreamTableScan { table: payloads, columns: [payloads.id, payloads.payload], stream_scan_type: SnapshotBackfill, stream_key: [payloads.id], pk: [id], dist: UpstreamHashShard(payloads.id) }
- name: two different SQL UDFs with FROM
  sql: |
    create function g_from(j jsonb) returns varchar
    language sql as $$ select y ->> 'k1' from (select j as y) t $$;
    create function h_from(j jsonb) returns varchar
    language sql as $$ select y ->> 'k2' from (select j as y) t $$;
    create table payloads (id int primary key, payload jsonb);
    select id, g_from(payload) as a, h_from(payload) as b from payloads;
  stream_plan: |-
    StreamMaterialize { columns: [id, a, b], stream_key: [id], pk_columns: [id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [payloads.id, JsonbAccessStr(payloads.payload, 'k1':Varchar) as $expr1, JsonbAccessStr(payloads.payload, 'k2':Varchar) as $expr2], noop_update_hint: true }
      └─StreamTableScan { table: payloads, columns: [payloads.id, payloads.payload], stream_scan_type: SnapshotBackfill, stream_key: [payloads.id], pk: [id], dist: UpstreamHashShard(payloads.id) }
- name: SQL UDF with FROM called once
  sql: |
    create function g_from(j jsonb) returns varchar
    language sql as $$ select y ->> 'k1' from (select j as y) t $$;
    create table payloads (id int primary key, payload jsonb);
    select id, g_from(payload) as a from payloads;
  stream_plan: |-
    StreamMaterialize { columns: [id, a], stream_key: [id], pk_columns: [id], pk_conflict: NoCheck }
    └─StreamProject { exprs: [payloads.id, JsonbAccessStr(payloads.payload, 'k1':Varchar) as $expr1], noop_update_hint: true }
      └─StreamTableScan { table: payloads, columns: [payloads.id, payloads.payload], stream_scan_type: SnapshotBackfill, stream_key: [payloads.id], pk: [id], dist: UpstreamHashShard(payloads.id) }
- name: SQL UDF whose FROM subquery can remove its row
  sql: |
    create function g_filtered(j jsonb) returns varchar
    language sql as $$ select y ->> 'k1' from (select j as y where j is not null) t $$;
    create table payloads (id int primary key, payload jsonb);
    select id, g_filtered(payload) as a, g_filtered(payload) as b from payloads;
  stream_error: |-
    Not supported: streaming nested-loop join
    HINT: The non-equal join in the query requires a nested-loop join executor, which could be very expensive to run. Consider rewriting the query to use dynamic filter as a substitute if possible.
    See also: https://docs.risingwave.com/processing/sql/dynamic-filters
- name: SQL UDF whose FROM subquery contains a shared impure expression
  sql: |
    create function g_impure(j jsonb) returns float8
    language sql as $$ select v + v from (select random() as v) t $$;
    create table payloads (id int primary key, payload jsonb);
    select id, g_impure(payload) as a, g_impure(payload) as b from payloads;
  optimized_logical_plan_for_batch: |-
    LogicalJoin { type: LeftOuter, on: true, output: all }
    ├─LogicalScan { table: payloads, columns: [payloads.id] }
    └─LogicalJoin { type: FullOuter, on: true, output: all }
      ├─LogicalProject { exprs: [($expr1 + $expr1) as $expr2] }
      │ └─LogicalProject { exprs: [Random as $expr1] }
      │   └─LogicalValues { rows: [[]], schema: Schema { fields: [] } }
      └─LogicalProject { exprs: [($expr3 + $expr3) as $expr4] }
        └─LogicalProject { exprs: [Random as $expr3] }
          └─LogicalValues { rows: [[]], schema: Schema { fields: [] } }
