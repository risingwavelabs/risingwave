[package]
name = "risingwave_logging"
version = "0.1.8"
edition = "2021"
# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
static-link = ["openssl/vendored", "isahc/static-curl", "isahc/static-ssl"]

[dependencies]
async-trait = "0.1"
futures = { version = "0.3", default-features = false, features = ["alloc"] }
isahc = { version = "1", default-features = false, features = ["http2", "text-decoding"] } # isahc is the http client used for tracing. Always set it as the same version as opentelemetry-jaeger's.
madsim = "=0.2.0-alpha.3"
openssl = "0.10"
opentelemetry = { version = "0.17", features = ["rt-tokio", "trace"] }
opentelemetry-jaeger = { version = "0.16", features = [
    "rt-tokio",
    "collector_client",
    "isahc",
    "isahc_collector_client",
] }
parking_lot = { version = "0.12", features = ["deadlock_detection"] }
thrift = "0.15" # thift is used for our implementation of `RwTokio` runtime. Always set it as the same version as opentelemetry-jaeger's.
tokio = { version = "=0.2.0-alpha.3", package = "madsim-tokio", features = [
    "rt",
    "rt-multi-thread",
    "sync",
    "macros",
    "time",
    "signal",
    "fs",
] }
tokio-stream = "0.1"
tracing = { version = "0.1" }
tracing-opentelemetry = "0.17"
tracing-subscriber = "0.3"
workspace-hack = { version = "0.1", path = "../../workspace-hack" }
