header:
  license:
    spdx-id: Apache-2.0
    copyright-owner: Singularity Data

  paths:
    - 'src/**/*.rs'
    # - 'legacy/**/*.java'
    # - 'legacy/**/*.kt'
    - 'dashboard/**/*.js'
    - 'src/**/*.html'

  comment: on-failure
