statement ok
SET RW_IMPLICIT_FLUSH TO true;

statement error unsupported data type
CREATE TABLE items (id bigint PRIMARY KEY, embedding vector(3));

query error unsupported data type
SELECT '[1,2,3]'::vector(3);

# More tests
