# CDC Postgres source validate test
control substitution on

# invalid username
statement ok
create source src_invalid_user with (
 connector = 'postgres-cdc',
 hostname = 'db',
 port = '5432',
 username = 'posres',
 password = 'postgres',
 database.name = 'cdc_test',
 slot.name = 'shipments'
);
statement error
create table shipments (
  shipment_id INTEGER,
  order_id INTEGER,
  origin STRING,
  destination STRING,
  is_arrived boolean,
 PRIMARY KEY (shipment_id)
) from src_invalid_user table 'shipments';

# invalid not null
statement ok
create source src_invalid_notnull with (
 connector = 'postgres-cdc',
 hostname = '${PGHOST:localhost}',
 port = '${PGPORT:5432}',
 username = '${PGUSER:$USER}',
 password = '${PGPASSWORD:}',
 database.name = '${PGDATABASE:postgres}',
 slot.name = 'shipments'
);
statement error CDC table with NOT NULL constraint is not supported
create table shipments (
  shipment_id INTEGER,
  order_id INTEGER,
  origin STRING,
  destination STRING NOT NULL,
  is_arrived boolean NOT NULL,
 PRIMARY KEY (shipment_id)
) from src_invalid_notnull table 'shipments';

# invalid password
statement ok
create source src_invalid_pwd with (
 connector = 'postgres-cdc',
 hostname = 'db',
 port = '5432',
 username = 'postgres',
 password = 'otgres',
 database.name = 'cdc_test',
 slot.name = 'shipments'
);
statement error
create table shipments (
  shipment_id INTEGER,
  order_id INTEGER,
  origin STRING,
  destination STRING,
  is_arrived boolean,
 PRIMARY KEY (shipment_id)
) from src_invalid_pwd table 'shipments';

# invalid table name
statement ok
create source src_invalid_table with (
 connector = 'postgres-cdc',
 hostname = 'db',
 port = '5432',
 username = 'postgres',
 password = 'postgres',
 database.name = 'cdc_test',
 slot.name = 'shipments'
);
statement error
create table shipments (
  shipment_id INTEGER,
  order_id INTEGER,
  origin STRING,
  destination STRING,
  is_arrived boolean,
 PRIMARY KEY (shipment_id)
) from src_invalid_table table 'shipment';

# invalid primary key
statement ok
create source src_invalid_pk with (
 connector = 'postgres-cdc',
 hostname = 'db',
 port = '5432',
 username = 'postgres',
 password = 'postgres',
 database.name = 'cdc_test',
 schema.name = 'public',
 slot.name = 'shipments'
);
statement error
create table shipments (
  shipment_id INTEGER,
  order_id INTEGER,
  origin STRING,
  destination STRING,
  is_arrived boolean,
 PRIMARY KEY (order_id)
) from src_invalid_pk table 'shipments';

# column name mismatch
statement ok
create source src_colname_mismatch with (
 connector = 'postgres-cdc',
 hostname = 'db',
 port = '5432',
 username = 'postgres',
 password = 'postgres',
 database.name = 'cdc_test',
 schema.name = 'public',
 slot.name = 'shipments'
);
statement error
create table shipments (
  shipment_id INTEGER,
  orderid INTEGER,
  origin STRING,
  destination STRING,
  is_arrived boolean,
 PRIMARY KEY (shipment_id)
) from src_colname_mismatch table 'shipments';

# column data type mismatch
statement ok
create source src_type_mismatch with (
 connector = 'postgres-cdc',
 hostname = 'db',
 port = '5432',
 username = 'postgres',
 password = 'postgres',
 database.name = 'cdc_test',
 schema.name = 'public',
 slot.name = 'shipments'
);
statement error
create table shipments (
  shipment_id INTEGER,
  order_id real,
  origin STRING,
  destination STRING,
  is_arrived boolean,
 PRIMARY KEY (shipment_id)
) from src_type_mismatch table 'shipments';

# format & encode provided and match with debezium json, this is okay
statement ok
create source src_format_encode with (
 connector = 'postgres-cdc',
 hostname = 'db',
 port = '5432',
 username = 'postgres',
 password = 'postgres',
 database.name = 'cdc_test',
 schema.name = 'public',
 slot.name = 'shipments'
);
statement ok
explain create table shipments (
  shipment_id INTEGER,
  order_id INTEGER,
  origin STRING,
  destination STRING,
  is_arrived boolean,
 PRIMARY KEY (shipment_id)
) from src_format_encode table 'shipments' format debezium encode json;

# format & encode provided but mismatch with debezium json, this is not allowed
statement ok
create source src_format_encode_err with (
 connector = 'postgres-cdc',
 hostname = 'db',
 port = '5432',
 username = 'postgres',
 password = 'postgres',
 database.name = 'cdc_test',
 schema.name = 'public',
 slot.name = 'shipments'
);
statement error Row format for CDC connectors should be either omitted or set to `FORMAT DEBEZIUM ENCODE JSON`
create table shipments (
  shipment_id INTEGER,
  order_id real,
  origin STRING,
  destination STRING,
  is_arrived boolean,
 PRIMARY KEY (shipment_id)
) from src_format_encode_err table 'shipments' format canal encode csv;

# Not supported: Non-generated column found after a generated column.
statement ok
create source src_gen_col_err with (
 connector = 'postgres-cdc',
 hostname = '${PGHOST:localhost}',
 port = '${PGPORT:5432}',
 username = '${PGUSER:$USER}',
 password = '${PGPASSWORD:}',
 database.name = '${PGDATABASE:postgres}',
 slot.name = 'shipments'
);
statement error Not supported: Non-generated column found after a generated column.
create table shipments (
  next_shipment_id INTEGER as shipment + 1,
  shipment_id INTEGER,
  order_id INTEGER,
  origin STRING,
  destination STRING,
  is_arrived boolean,
 PRIMARY KEY (shipment_id)
) from src_gen_col_err table 'shipments';

statement ok
create source src_numeric_to_rw_int256 with (
 connector = 'postgres-cdc',
 hostname = 'db',
 port = '5432',
 username = '${PGUSER:$USER}',
 password = '${PGPASSWORD:}',
 database.name = 'cdc_test',
 schema.name = 'public',
 slot.name = 'numeric_to_rw_int256'
);
statement ok
explain create table numeric_to_rw_int256 (
  id int,
  num rw_int256,
  PRIMARY KEY (id)
) from src_numeric_to_rw_int256 table 'public.numeric_table';

statement ok
create source src_numeric_to_varchar with (
 connector = 'postgres-cdc',
 hostname = 'db',
 port = '5432',
 username = '${PGUSER:$USER}',
 password = '${PGPASSWORD:}',
 database.name = 'cdc_test',
 schema.name = 'public',
 slot.name = 'numeric_to_varchar'
);
statement ok
explain create table numeric_to_varchar (
  id int,
  num varchar,
  PRIMARY KEY (id)
) from src_numeric_to_varchar table 'public.numeric_table';

statement ok
create source src_enum_to_varchar with (
 connector = 'postgres-cdc',
 hostname = '${PGHOST:localhost}',
 port = '${PGPORT:5432}',
 username = '${PGUSER:$USER}',
 password = '${PGPASSWORD:}',
 database.name = '${PGDATABASE:postgres}',
 slot.name = 'enum_to_varchar'
);
statement ok
explain create table enum_to_varchar (
  id int,
  current_mood varchar,
  PRIMARY KEY (id)
) from src_enum_to_varchar table 'enum_table';

# generated column
statement ok
create source src_shipments_gen_col with (
 connector = 'postgres-cdc',
 hostname = '${PGHOST:localhost}',
 port = '${PGPORT:5432}',
 username = '${PGUSER:$USER}',
 password = '${PGPASSWORD:}',
 database.name = '${PGDATABASE:postgres}',
 schema.name = 'public',
 slot.name = 'shipments'
);
statement ok
create table shipments (
  shipment_id INTEGER,
  order_id INTEGER,
  origin STRING,
  destination STRING,
  is_arrived boolean,
  generated_c INTEGER AS shipment_id + order_id,
 PRIMARY KEY (shipment_id)
) from src_shipments_gen_col table 'public.shipments';

statement ok
drop table shipments;

statement error Table 'partitioned_timestamp_table' has partitions, which requires publication 'rw_publication_pubviaroot_false' to be created with `publish_via_partition_root = true`.
CREATE TABLE IF NOT EXISTS partitioned_timestamp_table(
  c_int int,
  c_boolean boolean,
  c_timestamp timestamp,
  PRIMARY KEY (c_int, c_timestamp)
) WITH (
 connector = 'postgres-cdc',
 hostname = '${PGHOST:localhost}',
 port = '${PGPORT:5432}',
 username = '${PGUSER:$USER}',
 password = '${PGPASSWORD:}',
 database.name = '${PGDATABASE:postgres}',
 schema.name = 'public',
 publication.name = 'rw_publication_pubviaroot_false',
 slot.name = 'my_slot_partition'
);
