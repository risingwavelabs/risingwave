# Create a connection.
statement ok
CREATE CONNECTION conn0 WITH (type = 'mock');

# Create another user with duplicate name.
statement error
CREATE CONNECTION conn0 WITH (type = 'mock');

# Test quoting.
statement ok
CREATE CONNECTION "conn1" WITH (type = 'mock');

# Drop connections.
statement ok
DROP CONNECTION conn0;

# Drop connections.
statement ok
DROP CONNECTION conn1;
