onlyif can-use-recover
statement ok
create table t(v1 int);

onlyif can-use-recover
statement ok
insert into t select 1 from generate_series(1, 10000);

# trigger high barrier latency
onlyif can-use-recover
system ok
psql -c 'create materialized view m1 as select x.v1 from t x join t y using(v1);' &

# first create table
onlyif can-use-recover
system ok
psql -c 'create table t(v1 int);' &

onlyif can-use-recover
statement error table with name test exists but under creation
create table t(v1 int);

onlyif can-use-recover
statement ok
recover

onlyif can-use-recover
statement ok
drop table t;
