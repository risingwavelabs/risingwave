statement ok
create materialized source s (v1 int, v2 varchar) with ( 'connector' = 'kafka', 'kafka.topic' = 'kafka_1_partition_topic.1', 'kafka.brokers' = '127.0.0.1:29092', 'kafka.scan.startup.mode'='earliest' ) row format json

query IT rowsort
select * from s
----
1 1
2 22
3 333
4 4444

statement ok
drop source s
