statement ok
create stream s (v1 int not null, v2 char(8) not null) with ( upstream_source = 'kafka', kafka_topic = 'test_topic_1', kafka_bootstrap_servers = 'localhost:29092' ) row format json

query IT rowsort
select * from s
----
1 1
2 22
3 333
4 4444

statement ok
drop stream s

statement ok
create stream s (v1 int not null, v2 char(8) not null) with ( upstream_source = 'kafka', kafka_topic = 'test_topic_2', kafka_bootstrap_servers = 'localhost:29092' ) row format json

query IT rowsort
select * from s
----
1 1
2 22
3 333
4 4444

statement ok
drop stream s

statement ok
create stream s (v1 int not null, v2 char(8) not null) with ( upstream_source = 'kafka', kafka_topic = 'test_topic_3', kafka_bootstrap_servers = 'localhost:29092' ) row format json

query IT rowsort
select * from s
----
1 1
2 22
3 333
4 4444

statement ok
drop stream s

statement ok
create stream s (v1 int not null, v2 char(8) not null) with ( upstream_source = 'kafka', kafka_topic = 'test_topic_4', kafka_bootstrap_servers = 'localhost:29092' ) row format json

query IT rowsort
select * from s
----
1 1
2 22
3 333
4 4444

statement ok
drop stream s

statement ok
create stream s (v1 int not null, v2 char(8) not null) with ( upstream_source = 'kafka', kafka_topic = 'test_topic_100', kafka_bootstrap_servers = 'localhost:29092' ) row format json

query I
select count(*) from s
----
100

statement ok
drop stream s
