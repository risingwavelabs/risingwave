statement ok
CREATE TABLE t (v1 int);

statement ok
CREATE MATERIALIZED VIEW mv AS SELECT * FROM t;

statement ok
CREATE SINK snk FROM mv
    WITH (sink_type = 'kafka', kafka.brokers = '127.0.0.1:29092', kafka.topic = 'test_topic', sink.type = 'append_only');

statement ok
DROP SINK snk;

statement ok
DROP MATERIALIZED VIEW mv;

statement ok
DROP TABLE t;
