statement ok
create table t (v1 int, v2 numeric, v3 double, v4 interval);

statement ok
insert into t values
(1, 2, 3, interval '1' second),
(4, 5, 6, interval '1' minute),
(7, 8, 9, interval '1' hour);

statement ok
create materialized view mv_sum as
select
    sum(v1) as sum_v1,
    sum(v2) as sum_v2,
    sum(v3) as sum_v3,
    sum(v4) as sum_v4
from t;

statement ok
flush;

query I
select sum_v1, sum_v2, sum_v3, sum_v4 from mv_sum;
----
12  15  18  01:01:01

statement ok
drop materialized view mv_sum;

statement ok
drop table t;
