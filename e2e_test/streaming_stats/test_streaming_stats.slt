# Test streaming stats table functions
# These functions should return mock data to demonstrate functionality

statement ok
CREATE TABLE t1 (v1 int, v2 int);

statement ok
INSERT INTO t1 VALUES (1, 10), (2, 20), (3, 30);

# Test internal_get_channel_stats function
query TT
SELECT * FROM internal_get_channel_stats();
----
1	2	3	0.1	1000.0	950.0
2	3	2	0.05	950.0	900.0

# Test internal_get_fragment_stats function
query TT
SELECT * FROM internal_get_fragment_stats();
----
1	3	100
2	2	100
3	1	100

# Test internal_get_relation_stats function
query TT
SELECT * FROM internal_get_relation_stats();
----
1001	3	100
1002	2	100

# Clean up
statement ok
DROP TABLE t1;